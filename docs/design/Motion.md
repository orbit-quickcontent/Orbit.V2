# Orbit Motion & Transition Specifications

Extracted from Framer Motion page transition configs:
- **Screen Transitions**: `AnimatePresence mode="wait"`
- **Fade In / Out**: `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}`, `transition={{ duration: 0.2 }}`
- **Card Hover / Touch Scale**: `scale: 0.98` on press
- **Spring Curve**: Stiffness 300, Damping 30
