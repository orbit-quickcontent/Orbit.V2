{
  "name": "orbit-project",
  "version": "1.0.0",
  "private": true,
  "description": "Orbit Real-Time Dispatch and Mobile System",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "socket.io-client": "^4.7.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
