name: orbit_client
description: ORBIT client booking and real-time reel delivery app.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  http: ^1.2.0
  shared_preferences: ^2.2.3
  socket_io_client: ^2.0.3+1
  razorpay_flutter: ^1.4.5
  geolocator: ^14.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
